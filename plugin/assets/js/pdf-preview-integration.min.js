/**
 * PDF Preview Integration
 */

(function() {
  'use strict';

  console.log('[PDF Builder] PDF Preview Integration loaded');

  if (window.pdfPreviewAPI) {
    document.addEventListener('pdf-builder-preview', function(e) {
      console.log('[PDF Builder] Preview event triggered');
    });
  }
})();
