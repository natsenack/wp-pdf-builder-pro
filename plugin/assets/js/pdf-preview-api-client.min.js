/**
 * PDF Preview API Client
 */

window.pdfPreviewAPI = {
  generateEditorPreview: async function (templateData, options) {
    console.log("[PDF Builder] Generating editor preview");
    return { success: true };
  },
  generateOrderPreview: async function (templateData, orderId, options) {
    console.log("[PDF Builder] Generating order preview for order", orderId);
    return { success: true };
  },
};

console.log("[PDF Builder] PDF Preview API loaded");
