# 📋 Workflow de développement WP PDF Builder Pro

## Structure du projet

```
wp-pdf-builder-pro/
├── assets/js/                          # 📝 Sources JS/TS
│   ├── pdf-builder-react-wrapper.js    # Entry webpack
│   ├── pdf-builder-react/              # React components
│   ├── *.js                            # Scripts compilés
│   └── dist/                           # 🔨 Build webpack (generated)
│
├── plugin/resources/assets/js/         # 🚀 Production (copie du dist)
│   └── *.js                            # Fichiers compilés uniquement
│
└── build/
    └── deploy-simple.ps1               # Script déploiement
```

## Flux de développement

### 1️⃣ Développer
- Modifier fichiers dans `assets/js/`
- Fichiers TypeScript/JSX compilés automatiquement

### 2️⃣ Builder
```bash
npm run build  # Webpack compile assets/js/ → assets/js/dist/
```

### 3️⃣ Déployer
```bash
.\build\deploy-simple.ps1
# 1. Lance npm run build
# 2. Copie assets/js/dist/ → plugin/resources/assets/js/
# 3. Upload vers FTP
# 4. Git commit + push
```

## ⚠️ À NE PAS FAIRE

❌ Éditer les fichiers directement dans `plugin/resources/assets/js/`
❌ Mettre du code source dans `plugin/resources/assets/js/`
❌ Créer des dossiers `dist` en plusieurs endroits
❌ Déployer à la main

## 🧹 Nettoyage

Le plugin contient UNIQUEMENT les fichiers compilés par webpack :
- `ajax-throttle.js`
- `pdf-builder-react.js`
- `pdf-builder-wrap.js`
- `pdf-preview-api-client.js`
- `pdf-preview-integration.js`
- `settings-global-save.js`
- `tabs-root-monitor.js`

Tout le reste est généré par le build.

## 🔧 Config

- **webpack.config.cjs** - Entre : `assets/js/*.js` | Sortie : `assets/js/dist/`
- **deploy-simple.ps1** - Copie dist → plugin/resources/assets/js/
- **package.json** - `npm run build` lance webpack

---
**Version:** 2026-01-01 - Nettoyé et simplifié
